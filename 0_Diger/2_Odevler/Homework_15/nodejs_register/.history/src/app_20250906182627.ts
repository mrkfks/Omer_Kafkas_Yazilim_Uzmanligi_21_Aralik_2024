import express from 'express';

const app = express();
const PORT = 3000;

//EJS Configuration
app.set("vies")


app.listen(PORT, () => {
  console.log(`Sunucu http://localhost:${PORT} adresinde çalışıyor`);
});